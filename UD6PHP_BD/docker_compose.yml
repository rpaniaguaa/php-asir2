
services:
  php-apache:
    build: .  #path to Dockerfile
    image: php-apache8.4-pdo-img
    container_name: php8.4-apache-pdo-cont
    restart: always  
    ports:
      - "8080:80"
    volumes:
      - ./html:/var/www/html #DOCUMENT ROOT
    depends_on:
      - bd
    networks:
      - red_app_php

  bd:
    image: mariadb:10.11
    container_name: mariadb10_ud6
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: pwd_root_ud6
      MYSQL_DATABASE: unidad6
      MYSQL_USER: usuario_ud6
      MYSQL_PASSWORD: pwd_ud6
    volumes:
        - db_data:/var/lib/mysql    
    networks:
        - red_app_php
       
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: phpmyadmin-ud6
    environment:
      PMA_HOST: bd
      PMA_USER: root
      PMA_PASSWORD: pwd_root_ud6
    ports:
      - "8081:80"  
    depends_on:
      - bd
    networks:
        - red_app_php

networks:
  red_app_php:
    driver: bridge
volumes:
  db_data:
